# 우리가 매일 사용하는 ‘검색’의 이면

주소창에 `https://www.blog.naver.com` 을 입력하고 검색 버튼을 누르면,  
단 1초도 채 되지 않아 네이버 블로그 페이지가 눈앞에 나타난다.

우리는 이를 너무나 당연하게 받아들이지만,  
그 짧은 찰나의 순간 동안 수많은 네트워크 단계가 동시에 작동한다.

이 글은 그 ‘보이지 않는 순간’을 들여다본다.

사용자가 URL을 입력한 후, **브라우저가 어떻게 서버와 연결되어 웹 페이지를 화면에 표시하는지**,  
즉 브라우저와 서버가 어떻게 만나고 대화하는지를 단계별로 살펴본다.

---

## 🌐 웹의 목적

웹의 본질은 **정보 공유**다.  
팀 버너스리가 제안한 월드 와이드 웹(World Wide Web)은  
누구나 접근하고 정보를 나눌 수 있는 공간으로 설계되었다.

이를 가능하게 하는 기본 구조는 두 주체로 이루어진다.  
**서버(Server)** 와 **클라이언트(Client)** 이다.

우리가 사용하는 브라우저(Chrome, Safari 등)는 클라이언트의 대표적 형태이며,  
네이버나 구글 같은 웹사이트를 제공하는 시스템이 서버다.  
이 두 주체가 정보를 주고받기 위해서는 서로를 이어주는 통로가 필요하다.  
그 통로가 바로 **네트워크(Network)** 다.

---

## 🔗 네트워크란 무엇인가

![](https://velog.velcdn.com/images/wndkgus0225/post/2589a826-e76c-4c88-b110-084d6fa3809a/image.png)

네트워크는 클라이언트와 서버를 잇는 보이지 않는 다리다.  
하지만 이 단순한 정의 뒤에는 복잡한 과정이 숨어 있다.  
데이터를 패킷 단위로 나누고, 올바른 주소로 찾아가며, 손실 없이 순서대로 도착하도록 조정하는 체계가 존재한다.

즉, 네트워크는 **하나의 길**이 아니라  
수많은 약속(프로토콜)과 중간 거점(서버, 라우터)들이 얽혀 있는 **거대한 시스템**이다.

![](https://velog.velcdn.com/images/wndkgus0225/post/aa75897d-8dfe-4890-812e-b9203aa698a4/image.png)

브라우저가 URL을 입력받아 화면을 띄우기까지의 모든 과정은 이 네트워크 안에서 이루어진다.  
따라서 네트워크를 이해한다는 것은 웹의 동작 원리를 이해하는 출발점이다.

---

## 🧭 브라우저 - 서버 흐름

![](https://velog.velcdn.com/images/wndkgus0225/post/f17ee23a-bd34-4797-a24d-9e320cf3c0f6/image.png)

---

## 1️⃣ URL 해석

브라우저가 가장 먼저 마주하는 것은 사용자가 입력한 **URL (Uniform Resource Locator)** 이다.  
URL은 단순한 문자열이 아니라, 웹 상의 자원을 정확히 찾아가기 위한 **주소 체계**이다.

예시: https://blog.naver.com:443

이 주소에는 다음 세 가지 요소가 포함되어 있다.

| 구성 요소         | 예시                 | 설명                       |
| ----------------- | -------------------- | -------------------------- |
| 프로토콜 (Scheme) | `https://`           | 통신 방식 (HTTP, HTTPS 등) |
| 도메인 (Host)     | `www.blog.naver.com` | 서버의 이름 또는 위치      |
| 포트 번호 (Port)  | `:443`               | 서버 내부의 세부 경로      |

브라우저는 이 URL을 **파싱(Parsing)** 하여 각 요소를 분리하고,  
다음 단계에서 사용할 준비를 한다.

![](https://velog.velcdn.com/images/wndkgus0225/post/a26cb37f-90e5-456d-a5c0-2134099f626e/image.png)

도메인은 ‘서버가 어디 있는가’를,  
프로토콜은 ‘어떻게 통신할 것인가’를 결정한다.

---

## 2️⃣ DNS에 IP 질의

도메인(`blog.naver.com`)은 사람이 읽기 쉽도록 만들어진 이름일 뿐,  
컴퓨터는 이 문자열을 직접 이해하지 못한다.  
따라서 브라우저는 이 도메인을 실제로 연결 가능한 **IP 주소**로 변환해야 한다.

이 과정을 담당하는 시스템이 바로 **DNS (Domain Name System)** 이다.

![](https://velog.velcdn.com/images/wndkgus0225/post/5b5c635d-5f7e-43d6-8904-82512bd519f4/image.png)

브라우저는 DNS 서버에게 요청한다.

> “이 도메인에 해당하는 IP 주소를 알려줘!”

이 탐색은 다음과 같은 순서로 진행된다.

1. **루트 DNS 서버(Root)** — `.com` 등 상위 도메인의 위치를 알려줌
2. **TLD 서버(Top-Level Domain)** — `naver.com`의 위치를 찾아줌
3. **권한 DNS 서버(Authoritative)** — `blog.naver.com`의 정확한 IP 주소를 응답함

DNS는 이름 기반 요청을 숫자 기반 주소로 변환한다.  
그리고 브라우저는 이 결과를 **캐시에 저장**하여 다음 접속 시 더 빠르게 접근할 수 있게 한다.

---

### 포트 번호의 의미

![](https://velog.velcdn.com/images/wndkgus0225/post/8d0ffa11-b206-45a3-a046-c6c06368b6f1/image.png)

하나의 서버(IP)가 여러 서비스를 동시에 제공할 수 있기 때문에,  
**포트 번호**는 ‘세부 통신 창구’를 지정한다.

| 포트 번호   | 의미                         |
| ----------- | ---------------------------- |
| 80          | HTTP 서비스                  |
| 443         | HTTPS 서비스                 |
| 21          | FTP (파일 전송)              |
| 25          | SMTP (메일 전송)             |
| 3000 / 8080 | 개발용 서버 등 커스텀 서비스 |

`https://localhost:3000` → “내 컴퓨터의 3000번 포트에 있는 웹 서버와 통신하겠다”는 의미다.

포트 번호가 명시되지 않았다면, 브라우저는 프로토콜 기본값을 자동으로 사용한다.

| 프로토콜 | 기본 포트 |
| -------- | --------- |
| HTTP     | 80        |
| HTTPS    | 443       |

즉, `https://blog.naver.com` 은 실제로 `:443`이 생략된 형태다.

---

## 3️⃣ TCP 연결 — 안정적인 통로

![](https://velog.velcdn.com/images/wndkgus0225/post/5dba8d16-c044-4912-8633-142bacb9cd90/image.png)

서버와 데이터를 주고받기 위해서는 먼저 안정적인 연결 통로를 만들어야 한다.  
이 역할을 하는 프로토콜이 바로 **TCP (Transmission Control Protocol)** 이다.

TCP는 데이터의 **신뢰성**을 보장하기 위해 다음 기능을 수행한다.

- 데이터 손실 방지 (패킷 유실 시 재전송)
- 순서 보장 (Sequence Number로 순서 관리)
- 흐름 제어 및 혼잡 제어 (네트워크 상태에 따른 조절)

HTTP/1.0, 1.1, 2.0 모두 TCP 위에서 동작하며,  
HTTP/3부터는 일부 구조가 UDP 기반으로 확장되었지만 여전히 TCP가 표준이다.

---

### 🔄 3-Way Handshake

1. **SYN** — 브라우저 → 서버: “연결하고 싶어, 내 번호는 100이야.”
2. **SYN + ACK** — 서버 → 브라우저: “좋아, 내 번호는 200이야.”
3. **ACK** — 브라우저 → 서버: “너 것도 잘 받았어. 연결하자!”

이 세 단계를 통해 양측은 데이터를 주고받을 수 있는 **논리적 통로(Connection)** 를 확보한다.

---

## 4️⃣ TLS 연결 — 안전한 통로

![](https://velog.velcdn.com/images/wndkgus0225/post/7c0d5b88-6224-4638-83dd-91ca6e9aa1a8/image.png)

TCP가 ‘안정적인 통로’를 만든다면,  
**TLS (Transport Layer Security)** 는 ‘안전한 통로’를 만든다.

HTTP와 TLS를 결합하면 **HTTPS**가 된다.

TLS는 데이터를 암호화하여 **기밀성**, **무결성**, **인증**을 보장한다.

---

### TLS Handshake 요약

![](https://velog.velcdn.com/images/wndkgus0225/post/45e8a469-1db1-4926-b3f2-e0ac0ee2c720/image.png)

1. 브라우저가 “안전하게 통신하고 싶어!” 요청
2. 서버가 **SSL 인증서**(CA가 발급)를 전달
3. 브라우저가 인증서 검증 후 키 교환 정보 생성
4. 공개키/비공개키 방식을 이용해 **대칭키를 안전하게 합의**
5. 이후 모든 데이터는 이 대칭키로 암호화되어 전송됨

즉, **TLS = TCP의 신뢰성 + 보안성**

---

## 5️⃣ Request & Response

![](https://velog.velcdn.com/images/wndkgus0225/post/9fad95a5-6d69-4dc6-b590-582163edfa4c/image.png)

모든 준비가 끝난 브라우저는 서버로 **HTTP 요청(Request)** 을 보낸다.  
서버는 내부 로직을 수행하고 결과를 **HTTP 응답(Response)** 으로 돌려준다.

![](https://velog.velcdn.com/images/wndkgus0225/post/d48637ec-16c0-4ce5-9aba-bea5306f2462/image.png)

응답에는 HTML, CSS, JavaScript, 이미지 등 다양한 콘텐츠가 포함된다.  
브라우저는 이를 해석하고 필요한 추가 리소스를 다시 요청한다.

이 과정을 거쳐 최종적으로 화면에 보이는 웹 페이지가 **렌더링(Rendering)** 된다.

---

## ⚙️ 우리가 체감하지 못하는 찰나의 과정

![](https://velog.velcdn.com/images/wndkgus0225/post/40a7a00e-7695-4d4e-b58a-0425dbd956af/image.png)

이 모든 단계는 사용자가 검색 버튼을 클릭한 후  
**수백 밀리초(ms)** 내에 수행된다.

즉, 우리가 ‘순식간에 뜨는 웹페이지’를 보는 그 순간,  
브라우저는 수십 개의 요청과 응답, 수천 번의 연산을 동시에 처리하고 있는 것이다.

---

즉 정리하자면,

| 핵심 요소  | 역할                                               |
| ---------- | -------------------------------------------------- |
| **정확성** | DNS와 TCP가 올바른 서버와 안정적으로 연결되도록 함 |
| **보안성** | TLS가 데이터의 무결성과 기밀성을 보장              |
| **가시성** | 브라우저가 데이터를 시각적으로 표현                |

결국, 웹은 단순한 기술의 조합이 아니라  
**“정보를 안전하게 공유하기 위한 인간과 기계의 협력 체계”** 다.

브라우저와 서버는 오늘도 그 협력을 이어가며,  
우리에게 매 순간 빠르고 믿을 수 있는 정보를 전달한다.
