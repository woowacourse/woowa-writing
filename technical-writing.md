# React Compiler

## 들어가며

React를 사용하다 보면 주기적으로 등장하는 화두가 있습니다.

바로 “**`useMemo`나 `useCallback`은 언제 사용하는 게 좋을까?**”라는 질문이죠.

공식 문서에서도 예를 들어, 아래처럼 커스텀 훅을 만들 때 `useCallback`을 사용하여 함수가 매 렌더링마다 재생성되지 않도록 최적화하는 것이 좋다고 말합니다. ([공식 문서 참고](https://ko.react.dev/reference/react/useCallback#optimizing-a-custom-hook))

하지만 이외의 상황에서는 명확한 기준을 찾기 어렵습니다. 어떤 개발자는 적극적으로 사용하는 편이고, 어떤 팀은 불필요한 복잡성을 만든다고 하며 거의 쓰지 않기도 합니다.

이처럼 `useMemo`, `useCallback`, `React.memo`를 “언제” 사용하는 것이 최적인지 판단하는 일은 단순한 문법 이해를 넘어 **개발자의 경험, 코드 스타일, 상황에 따라 달라지기 때문에 생각보다 어렵습니다.** 저 역시 프로젝트를 진행하며 매번 이 선택 앞에서 고민하곤 했습니다.

그런데 이런 고민을 근본적으로 줄일 수 있는 새로운 접근이 등장했습니다. 바로 **React Compiler**입니다.

React Compiler는 기존처럼 개발자가 일일이 수동으로 메모이제이션을 설정하지 않아도, 컴파일 단계에서 **자동으로 최적화를 적용**해줍니다. 코드를 그대로 유지한 채로도 성능 최적화를 가능하게 한다는 점에서 매우 흥미로운 변화이며, 저는 이 새로운 접근 방식이 어떻게 동작하는지 깊이 파보고 싶어졌습니다.

## React의 렌더링 구조와 최적화의 필요성

React 애플리케이션은 기본적으로 **부모 컴포넌트가 리렌더링되면 자식 컴포넌트도 함께 리렌더링**됩니다.

이는 선언형 UI의 일관성을 유지하기 위한 React의 설계 철학 때문입니다.

즉, 데이터가 바뀌면 트리 전체를 다시 그려 “항상 최신 상태를 화면에 반영”하도록 보장하는 것이죠.

![image.png](./image.png)

하지만 이 구조는 상황에 따라 **불필요한 리렌더링**을 유발하기도 합니다. 예를 들어, 부모의 상태 변경이 자식의 UI에는 영향을 주지 않는 경우에도 자식이 다시 렌더링될 수 있습니다. 이러한 불필요한 렌더링은 곧 성능 저하로 이어집니다.

이를 방지하기 위해 React에서는 다음과 같은 **수동 최적화 기법**을 제공합니다.

- `React.memo` – props가 변경되지 않았다면 자식 컴포넌트 전체의 리렌더링을 건너뜁니다.
- `useMemo` / `useCallback` – 특정 값이나 함수의 **재계산을 방지**해, 연산 비용이 큰 로직의 실행을 최소화합니다.

즉, 컴포넌트 전체의 리렌더링을 막는 방법(`React.memo`)과, 로직 수준에서의 재계산을 줄이는 방법(`useMemo`, `useCallback`)을 통해 성능을 개선할 수 있습니다.

하지만 앞서 말했듯 이 모든 과정은 개발자가 직접 코드를 작성하고 의존성을 신중히 관리해야 합니다. 조금만 실수해도 최적화 효과를 제대로 얻지 못하거나, 오히려 코드 복잡도만 높아질 수 있습니다.

## React Compiler 원리와 동작 방식

### React Compiler란 무엇인가

React Compiler는 React 팀이 새롭게 도입한 **자동 메모이제이션 컴파일러**입니다.

기존의 React는 부모 컴포넌트가 리렌더링될 때 모든 자식 컴포넌트도 다시 렌더링되는 구조를 가지고 있습니다. 이로 인해 불필요한 연산이 발생하고, 성능 최적화를 위해 개발자가 직접 `React.memo`, `useMemo`, `useCallback`과 같은 도구를 사용해야 했습니다.

그러나 이런 수동 최적화는 몇 가지 한계를 가지고 있습니다.

- “언제 사용하는 것이 적절한가?”에 대한 명확한 기준이 없고
- 의존성 배열을 잘못 지정하거나 적용을 빠뜨리면 최적화 효과를 얻지 못하며
- 코드 복잡도를 높일 수 있습니다.

React Compiler는 이러한 문제를 해결하기 위해 만들어졌습니다.

컴파일 단계에서 코드를 **정적으로 분석하여 자동으로 메모이제이션을 적용**하기 때문에, 기존 코드의 형태를 유지한 채 성능을 최적화할 수 있습니다. 즉, 개발자가 “여기서 memo를 써야 하나?”를 고민하는 대신, **React가 스스로 안전하고 효과적인 최적화를 수행**하게 되는 것이죠.

### 기존 방식과 달라진 점

기존 React 애플리케이션에서는 성능 최적화를 위해 **개발자가 직접 메모이제이션을 작성**해야 했습니다.

- `React.memo`: props가 변하지 않으면 컴포넌트 리렌더링을 방지
- `useMemo`: 계산 결과를 재사용
- `useCallback`: 함수 인스턴스를 재사용

하지만 이 방식은 개발자의 판단에 따라 결과가 달라지며, 팀마다 사용 기준도 제각각입니다. 잘못 사용하면 오히려 성능이 악화될 수도 있습니다.

React Compiler는 이 과정을 자동화합니다.

- 코드를 분석해 **의존성을 자동으로 추론**하고,
- **안전한 범위에서만 메모이제이션을 적용**하며,
- 기존 코드 스타일을 유지하면서도 성능을 개선합니다.

결과적으로 개발자는 비즈니스 로직에 집중할 수 있고, 최적화는 React가 책임지게 됩니다.

### 메모이제이션 동작 방식 이해하기

React Compiler가 동작하는 핵심은 **정적 분석(Static Analysis)**입니다.

코드를 AST(Abstract Syntax Tree, 추상 구문 트리) 형태로 분석해 어떤 값이 변경될 수 있고, 어떤 값이 불변인지 추론합니다.

이 과정을 통해 “이 계산은 다시 수행할 필요가 없다”는 것을 판단하면, React Compiler는 그 부분을 자동으로 메모이제이션합니다.

예를 들어, 어떤 컴포넌트가 props로 전달받은 값만을 사용한다면, Compiler는 이 값이 변하지 않는 한 **컴포넌트 전체를 다시 렌더링할 필요가 없다**고 판단합니다.

반대로, 외부 상태나 사이드이펙트를 사용하는 로직이라면 안전하지 않다고 판단해 자동 최적화에서 제외합니다.

즉, React Compiler는 **안전성을 유지하면서도 성능을 극대화하는 최적화 전략을 자동으로 선택**하는 것입니다.

### 코드 예제로 살펴보기

React Compiler가 실제로 어떻게 동작하는지 이해하기 위해 간단한 예제를 살펴보겠습니다.

아래 코드는 단순히 `value`를 받아서 **매우 비싼 연산 함수**인 `엄청_비싼_계산()`의 결과를 화면에 표시하는 컴포넌트입니다.

```tsx
type Props = {
  value: '딸기' | '당근' | '수박';
};

const A = ({ value }: Props) => {
  return <div>{엄청_비싼_계산(value)}</div>;
};

```

이 코드만 놓고 보면, `value`가 바뀔 때마다 컴포넌트 전체가 다시 렌더링되고 `엄청_비싼_계산()`도 다시 실행됩니다.

하지만 React Compiler는 **이 함수가 동일한 입력(`value`)에 대해서는 항상 동일한 결과를 낸다**는 것을 정적 분석을 통해 추론하고, 이를 자동으로 메모이제이션합니다.

즉, `value`가 이전과 같다면 **이미 계산한 결과를 재사용**하고, 그렇지 않은 경우에만 다시 계산하도록 코드를 변환합니다.

React Compiler가 실제로 생성하는 코드는 다음과 같습니다:

```tsx
const A = (t0) => {
  const $ = _c(4);
  const { value } = t0;
  let t1;
  if ($[0] !== value) {
    t1 = 엄청_비싼_계산(value);
    $[0] = value;
    $[1] = t1;
  } else {
    t1 = $[1];
  }
  let t2;
  if ($[2] !== t1) {
    t2 = <div>{t1}</div>;
    $[2] = t1;
    $[3] = t2;
  } else {
    t2 = $[3];
  }
  return t2;
};

```

여기서 `_c(4)`는 **4개의 슬롯을 가진 캐시 저장소**를 의미하며, `$[0]`에는 이전 입력값, `$[1]`에는 이전 계산 결과가 저장됩니다.

즉, 입력(`value`)이 이전과 동일하다면 `엄청_비싼_계산()`을 다시 호출하지 않고 `$[1]`의 결과를 재사용합니다.

이처럼 React Compiler는 개발자가 `useMemo`를 직접 쓰지 않아도 **자동으로 연산 결과를 캐시하고 재활용**합니다.

---

하지만 같은 UI를 조금 다르게 작성하면 이야기가 달라집니다.

아래는 조건문을 사용해 `value` 값에 따라 서로 다른 분기를 가지도록 작성한 코드입니다.

```tsx
type Props = {
  value: '딸기' | '당근' | '수박';
};

const B = ({ value }: Props) => {
  if (value === '딸기') {
    return <div>{엄청_비싼_계산(value)}</div>;
  } else if (value === '당근') {
    return <div>{엄청_비싼_계산(value)}</div>;
  } else {
    return <div>{엄청_비싼_계산(value)}</div>;
  }
};

```

UI는 동일하게 동작하지만, React Compiler가 변환한 코드를 보면 **메모이제이션 구조가 훨씬 복잡해진 것**을 확인할 수 있습니다.

```tsx
const B = (t0) => {
  const $ = _c(12);
  const { value } = t0;
  if (value === "딸기") {
    let t1;
    if ($[0] !== value) {
      t1 = 엄청_비싼_계산(value);
      $[0] = value;
      $[1] = t1;
    } else {
      t1 = $[1];
    }
    let t2;
    if ($[2] !== t1) {
      t2 = <div>{t1}</div>;
      $[2] = t1;
      $[3] = t2;
    } else {
      t2 = $[3];
    }
    return t2;
  } else {
    if (value === "당근") {
      let t1;
      if ($[4] !== value) {
        t1 = 엄청_비싼_계산(value);
        $[4] = value;
        $[5] = t1;
      } else {
        t1 = $[5];
      }
      let t2;
      if ($[6] !== t1) {
        t2 = <div>{t1}</div>;
        $[6] = t1;
        $[7] = t2;
      } else {
        t2 = $[7];
      }
      return t2;
    } else {
      let t1;
      if ($[8] !== value) {
        t1 = 엄청_비싼_계산(value);
        $[8] = value;
        $[9] = t1;
      } else {
        t1 = $[9];
      }
      let t2;
      if ($[10] !== t1) {
        t2 = <div>{t1}</div>;
        $[10] = t1;
        $[11] = t2;
      } else {
        t2 = $[11];
      }
      return t2;
    }
  }
};

```

앞선 A 컴포넌트에서는 `$[0]`과 `$[1]`만으로 입력값과 결과를 추적했지만, B 컴포넌트에서는 **조건문 분기마다 별도의 캐시 슬롯을 생성**해 각각의 메모이제이션을 따로 관리하고 있습니다.

이는 Compiler가 “조건에 따라 서로 다른 계산 경로가 존재한다”고 판단했기 때문입니다.

결과적으로 **계산 로직의 입력 구조가 단순할수록 Compiler의 메모이제이션 전략도 단순하고 효율적으로 동작**합니다.

반대로, 조건문이 많거나 분기별로 다른 코드 경로를 가진다면 Compiler는 안전성을 확보하기 위해 더 많은 메모이제이션 로직을 생성하며, 이는 곧 **번들 크기의 증가와 복잡한 캐시 관리 구조**로 이어질 수 있습니다.

<aside>
💡 핵심 포인트 정리

- React Compiler는 입력값이 변하지 않은 연산을 자동으로 캐시하여 재실행을 방지한다.
- 코드 구조가 단순할수록 메모이제이션 전략도 단순하고 효율적으로 동작한다.
- 동일한 UI라도 조건문, 분기, 표현 방식 등에 따라 Compiler의 판단과 생성되는 코드 구조가 달라진다.
</aside>

이 섹션에서 우리가 얻을 수 있는 중요한 교훈은 하나입니다. **자동 최적화를 믿더라도 “코드를 어떻게 작성하느냐”는 여전히 중요하다**는 것입니다. React Compiler는 수동 최적화의 수고를 줄여주지만, 더 나은 성능을 위해서는 여전히 **불필요한 조건문을 줄이고 의존성을 명확히 표현하는 코드 스타일**이 필요합니다.

## 주의사항

React Compiler는 분명 강력한 도구이지만, **모든 문제를 마법처럼 해결해주는 만능 열쇠는 아닙니다.** 실제 프로젝트에 적용할 때는 몇 가지 중요한 점을 반드시 염두에 두어야 합니다.

---

### 1. 번들 크기가 증가할 수 있다

React Compiler는 메모리 사용량을 늘리지 않으면서도 연산 결과를 캐시하는 방식으로 동작합니다. 하지만 이를 위해 **컴파일 과정에서 코드가 더 많은 형태로 변환**되기 때문에, 결과적으로 **번들 크기가 커질 수 있습니다.**

예를 들어, 앞서 살펴본 예제처럼 동일한 UI를 다른 구조로 작성했을 때, Compiler가 메모이제이션 전략을 달리 적용하며 더 많은 분기 처리를 삽입할 수도 있습니다.

이는 기능적으로 문제가 되지는 않지만, 대규모 프로젝트에서는 **번들 크기 증가 → 초기 로드 시간 증가**로 이어질 수 있으므로 주의가 필요합니다.

따라서 React Compiler를 도입하더라도 “자동 최적화가 있으니 아무렇게나 작성해도 된다”는 접근은 위험합니다.

**코드 구조를 명확히 하고 불필요한 의존성을 최소화하는 습관**이 여전히 중요합니다.

---

### 2. 코드 작성 방식에 따라 결과가 달라진다

React Compiler는 정적 분석을 통해 메모이제이션 여부를 판단합니다.

즉, **코드의 표현 방식**이 다르면 **Compiler의 해석도 달라질 수 있습니다.**

- 불필요하게 의존성을 추가하거나
- 외부 상태나 사이드이펙트를 참조하거나
- 값의 안정성을 보장할 수 없는 방식으로 코드를 작성하면

Compiler는 이를 안전하지 않다고 판단하고 최적화를 적용하지 않을 수 있습니다.

이는 React Compiler가 “자동화”를 지향하더라도 여전히 **개발자의 코드 스타일과 작성 습관에 따라 결과가 달라질 수 있다**는 것을 의미합니다.

---

### 3. 자동 최적화의 한계를 이해해야 한다

React Compiler는 의존성 분석과 메모이제이션을 자동화하지만, 이는 어디까지나 **React 규칙을 따르는 안전한 코드**를 전제로 합니다.

예를 들어, 상태를 직접 변경(mutation)하거나, 렌더링 도중 부작용(side effect)을 일으키는 코드에서는 자동 최적화가 적용되지 않습니다.

또한 Compiler가 모든 계산 비용을 줄여주는 것은 아니며, 일부 상황에서는 기존의 `useMemo`나 `React.memo`를 명시적으로 사용하는 것이 더 의도가 분명하고 효율적일 수 있습니다.

---

✅ **정리하자면**, React Compiler는 우리가 일일이 판단하던 “최적화 타이밍”을 자동으로 관리해주는 강력한 도구이지만, 그 효과는 코드의 구조와 작성 방식에 크게 의존합니다.

자동 최적화를 믿되, **번들 크기와 코드 표현 방식, 안전성의 전제를 항상 염두에 두는 것**이 필요합니다.

---

## 마치며

React Compiler는 단순히 새로운 기능 이상의 의미를 지닙니다.

그동안 우리는 `useMemo`, `useCallback`, `React.memo`를 언제, 어디서 적용할지 고민하며 수동으로 최적화를 관리해왔습니다. 이제 React Compiler는 이 과정을 자동화해줌으로써 **개발자가 로직에 집중할 수 있는 환경**을 만들어줍니다.

이 변화는 성능 최적화의 패러다임을 “개발자의 판단”에서 “컴파일러의 분석”으로 이동시킵니다.

물론 자동화된 최적화가 만능은 아니지만, 우리가 더 이상 세밀한 성능 트릭에 시간을 쓰지 않아도 된다는 점은 분명합니다. 그 시간과 에너지를 사용자 경험 개선이나 새로운 기능 개발에 쓸 수 있다면, 그것이야말로 진정한 생산성 향상일 것입니다.
