원정대 공유회 발표 회고
원정대 활동
레벨1 동안 원정대 활동이 진행됐다. 원정대는 단순한 스터디가 아니라 하나의 주제를 좁고 깊게 탐색해 해당 분야의 전문가가 된 뒤, 공유회를 통해 다른 크루들에게 이를 전이하는 활동이다. 나만의 송곳을 만드는 것이 목표였다.
나는 자바스크립트 실행 컨텍스트 원정대와 함수형 프로그래밍 원정대, 두 곳에 참여했다. 바쁘긴 했지만 양쪽 원정대원들의 배려 덕분에 공유회 준비까지 어찌저찌 잘 마쳤다.
함수형 원정대에서는 레스, 유월, 파라디, 도넛이 두 세션으로 나눠 발표를 준비했다. 실행 컨텍스트 원정대의 두 번째 공유회 세션에서는 루멘, 유월과 함께 각각 스코프, 실행 컨텍스트, 클로저를 맡았다.
클로저는 실행 컨텍스트를 이해했다면 어렵지 않게 이해할 수 있는 개념이었고, 스스로도 실행 컨텍스트를 잘 알고 있다고 생각했다. 준비된 자료와 스크립트만 잘 숙지하면 충분하다고 여겨, 가벼운 마음으로 세션을 준비했다.
공유회 당일
어느덧 공유회 날이 됐다. 두 번째 세션 시간이 다가오자 크루들이 하나둘 모여들기 시작했는데, 눈에 띄는 한 명이 있었다. 바로 포비였다. 예상하지 못한 일이라 살짝 당황했지만, 준비한 대로 발표를 진행했다.
루멘의 스코프 파트가 끝나고 유월의 실행 컨텍스트 파트가 시작됐다. 스코프와 클로저를 포함해 이 세션 전체를 관통하는 개념이 실행 컨텍스트였던 만큼, 이 파트에서 많은 질문이 쏟아졌다. 그중 원정대원 전체를 당황하게 한 질문이 하나 있었다.

> 코드의 평가 단계에서 식별자가 수집되고, 실제 값은 런타임 단계에서 할당된다고 했는데, 함수가 호출되려면 이름만 수집되어서는 안 될 것 같다. 평가 단계에서 정말 함수의 이름만 수집되는지, 다른 정보는 수집되지 않는 것인지 궁금하다.

이 질문을 이해하려면 자바스크립트 코드의 실행과 실행 컨텍스트에 대한 이해가 필요하다. 우리는 공유회에서 아래와 같이 설명했었다.

자바스크립트 코드의 실행은 평가와 런타임의 두 단계로 나뉜다.
평가 단계에서는 실행 컨텍스트와 내부 렉시컬 환경의 레코드가 생성되고, 식별자(변수, 함수)가 수집되어 등록된다.
런타임 단계에서는 등록된 식별자에 실제 값이 할당된다.

이 설명대로라면 평가 단계에서 함수의 이름만 수집되는 게 맞다. 다만 아무도 예상하지 못했던 지점을 찌른 질문이었고, 결국 제대로 답하지 못한 채 다음 파트로 넘어갔다.
클로저 발표, 그리고 두 번째 질문
내 발표 차례가 됐다. 주제는 클로저였다.

```jsx
function createCounter() {
  let count = 0;
  return () => ++count;
}

const counter = createCounter();

console.log(counter()); // 1
console.log(counter()); // 2
```

(클로저 설명 채울 부분)
설명이 끝나자 포비의 질문이 또 들어왔다.
중첩 함수가 어떻게 외부 함수의 변수를 참조할 수 있냐는 것이었다.
포비는 화이트보드에 콜스택을 그리며, 외부 함수의 변수를 참조하려면 콜스택에서 중첩 함수의 실행 컨텍스트가 어디에 놓여야 하냐고 물었다.
나는 클로저가 중첩 함수가 외부 함수보다 긴 생명 주기를 가지는 경우에 성립한다는 것을 알고 있었다. 외부 함수의 실행 컨텍스트가 먼저 팝되고, 그 이후 중첩 함수의 실행 컨텍스트가 콜스택 최상단에 놓인다고 설명했어야 했다. 하지만 당황한 나머지 앞부분을 빠뜨리고 "콜스택 최상단에 중첩 함수가 놓여야 한다"고만 답했다.
당연히 불완전한 설명이었고, 부끄럽게도 공유회를 듣기 위해 온 아지가 대신 올바른 답변을 해줬다.
이런저런 질문이 이어지며 세션이 길어졌고, 다른 크루들과 코치들은 밖에서 기다리기 시작했다. 제대로 답변하지 못했다는 생각에 어벙벙한 상태로 클로저 문제를 같이 풀고 세션을 마쳤다.
가짜 이해
발표를 준비할 때는 분명 스코프, 실행 컨텍스트, 클로저를 제대로 이해하고 있다고 생각했다. 그런데 막상 질문 앞에서는 말이 나오지 않았다.
내가 알고 있다고 믿었던 것들은, 사실 누군가에게 설명하거나 반박에 대응할 수 있을 만큼 이해한 것이 아니었다. 읽고 정리한 것을 이해했다고 착각한 것이었다.

(추가 작성이 필요한 부분)
