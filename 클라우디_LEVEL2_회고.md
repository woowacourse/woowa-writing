# 레벨2 비동기 회고

지난 비동기 수업에서는 말미에 이런 맥락의 질문이 있었다.

> 콜백, 프로미스, async/await 코드는 실행 컨텍스트 관점에서 동일하게 동작할까?

당시 나는 실행 컨텍스트에 대해 어느 정도 자신감이 있었다. 원정대에서 관련 내용을 공부하기도 했고, 공유회에서 꼬리 질문을 받으며 개념을 정리해본 경험도 있었기 때문이다. 그래서 나는 자신 있게 콜백은 비동기 작업이 끝나면 이후 작업이 태스크 큐에 등록되어 콜 스택과 마이크로태스크 큐가 비어 있을 때 콜 스택에 올라오고, 프로미스는 비동기 작업이 끝나면 이후 동작이 마이크로태스크 큐에 올라가 콜 스택이 비면 콜 스택에 올라온다고 내용을 작성하기 시작했다.

그런데 막상 async/await을 실행 컨텍스트 관점에서 설명하려는 순간 머릿속이 하얘졌다. 내가 async/await에 대해 알고 있던 것은 비동기 코드를 동기 코드처럼 작성할 수 있다는 것, 그리고 흔히 “Promise의 문법 설탕”이라고 불린다는 것 정도였다. 그 표현이 실제로 무엇을 의미하는지, 특히 `await`을 만났을 때 실행 컨텍스트가 어떻게 되는지는 알지 못했다.

모르는 지점을 발견했지만, 숨기고 싶다는 생각보다는 크루들과 함께 이야기해보고 싶다는 생각이 먼저 들었다. 그래서 수업이 끝난 뒤 평소 실행 컨텍스트 관련으로 자주 이야기를 나누던 크루들과 자연스럽게 이야기를 시작했다.

이때 우리는 가지고 있는 배경지식을 총동원해 가설을 세우고 확인해보는 방식으로 접근했다. 먼저 async/await을 설명할 때 자주 사용되는 “async/await은 Promise의 문법 설탕이다”라는 문장에서 추리를 시작했다. “async/await이 Promise의 문법 설탕이라면 단순히 Promise then 체인으로 바뀌지 않을까?”라는 가설을 세웠다.

우리가 설명하려던 코드는 대략 이런 형태였다.

```js
async function run() {
  const user = await getUser();
  const posts = await getPosts(user.id);
  const comments = await getComments(posts[0].id);

  console.log(user.name, comments[0].content);
}
```

처음에는 단순히 await을 then으로 치환해보았다.

```js
function run() {
  return getUser()
    .then((user) => {
      return getPosts(user.id);
    })
    .then((posts) => {
      return getComments(posts[0].id);
    })
    .then((comments) => {
      console.log(user.name, comments[0].content);
    });
}
```

하지만 막상 이렇게 바꿔보니 마지막 then에서 이전 단계의 user 값에 접근할 수 없는 문제가 생겼다. user는 첫 번째 then 콜백의 매개변수였기 때문에, 마지막 then 콜백의 스코프에서는 사용할 수 없었다. 그래서 then을 중첩하는 방식으로 다시 바꿔보았고, 그제야 실행 결과는 async/await 코드와 비슷하게 나왔다.

```js
function run() {
  return getUser().then((user) => {
    return getPosts(user.id).then((posts) => {
      return getComments(posts[0].id).then((comments) => {
        console.log(user.name, comments[0].content);
      });
    });
  });
}
```

이렇게 하면 실행 결과는 async/await 코드와 비슷하게 만들 수 있었다. 하지만 그 과정에서 “결과를 비슷하게 만들 수 있다”는 것과 “같은 구조로 실행된다고 말할 수 있다”는 것은 다른 문제일 수 있겠다는 생각이 들었다.

then 콜백은 각각 별도의 함수이고, Promise가 처리될 때마다 새로운 콜백이 마이크로태스크 큐에 들어간다. 그때마다 새로운 실행 컨텍스트가 만들어진다. 반면 async/await 코드는 하나의 함수 흐름 안에서 멈췄다가 이어지는 것처럼 보인다. 겉으로 비슷하게 동작한다고 해서 내부 동작까지 같다고 단정할 수 없다는 점이 보이기 시작했다. 첫 번째 가설은 async/await의 일부를 설명해주었지만, 실행 컨텍스트 관점의 질문에 충분히 답해주지는 못하고 있었다.

그 다음에는 await을 만나면 실행 컨텍스트가 콜 스택에 남아서 Promise가 끝나기를 기다리는 것 아닐까 하는 가설을 세워보았다. 처음에는 그럴듯해 보였다. 실행 컨텍스트가 그대로 남아 있다면 지역 변수도 유지될 것이고, Promise가 끝난 뒤 같은 자리에서 이어서 실행할 수 있을 것 같았기 때문이다.

하지만 이 가설도 코드를 실행해보며 깨졌다.

```js
console.log('start');
run();
console.log('end');
```

만약 await이 콜 스택을 막고 기다린다면, run 함수가 끝난 뒤에야 `end`가 출력되어야 한다. 하지만 실제 출력은 달랐다.

```text
start
end
클라우디
```

run 함수를 호출한 뒤 바로 다음 줄의 console.log가 먼저 실행된 것이다. await이 콜 스택을 막고 있었다면 이런 출력 순서는 나올 수 없었다.

그렇다면 await은 콜 스택을 막지 않으면서도 어떻게 멈췄던 지점부터 다시 이어서 실행될 수 있을까? 이 질문을 따라가다 보니 제너레이터를 떠올리게 되었다. 제너레이터는 일반 함수와 달리 실행을 중간에 멈췄다가 다시 이어갈 수 있는 함수다.

```js
function* gen() {
  console.log('start');
  const user = yield 'pause';
  console.log('resumed ' + user.name);
}

const iterator = gen();

iterator.next();
// start 출력
// { value: 'pause', done: false }

iterator.next({ name: '클라우디' });
// resumed 클라우디
```

제너레이터는 `yield`를 만나면 실행을 멈추고 제어권을 호출자에게 돌려준다. 이때 콜 스택을 계속 점유하지는 않지만, 함수가 어디까지 실행되었는지와 그 시점의 지역 상태는 사라지지 않는다. 이후 다시 `next(value)`를 호출하면 멈춰 있던 `yield` 표현식의 결과로 `value`가 들어오고, 함수는 그 지점부터 이어서 실행된다.

이 구조가 우리가 마주한 상황과 닮아 있다고 느꼈다. `await`도 콜 스택을 계속 점유하지는 않지만, Promise가 처리된 뒤에는 멈췄던 지점부터 다시 이어져야 한다. 물론 async/await이 실제로 제너레이터와 동일하게 동작한다고 말하려는 것은 아니다. 다만 제너레이터의 중단과 재개 모델은 `await`이 어떻게 하나의 흐름처럼 이어질 수 있는지 이해하는 데 좋은 실마리가 되었다.

돌아보면 이 과정에서 틀린 추측을 해보는 일이 생각보다 중요했다. `then`으로 단순히 바꿔보는 시도도 완전하지 않았고, `await`이 콜 스택에 남아 기다린다는 생각도 맞지 않았다. 하지만 그 가설들이 무의미했다고 느껴지지는 않았다. 오히려 하나씩 확인하고 깨뜨리는 과정에서 내가 무엇을 모르고 있었는지가 조금씩 더 명확해졌다.

이 과정이 즐거웠던 이유는 정답을 빠르게 찾았기 때문만은 아니었다. 크루들과 이야기할 때는 완성된 답을 말해야 한다기보다, 지금 떠오른 생각을 일단 꺼내놓고 함께 확인해보는 분위기가 있었다. 누군가의 질문은 내 생각을 다른 방향으로 열어주었고, 농담처럼 지나갔던 표현도 나중에는 개념을 이해하는 실마리가 되었다.

언젠가 레스가 함수가 어떻게 중간에 멈췄다가 다시 이어질 수 있는지를 이야기하며 “함수가 냉동 수면하는 것 같은 건가?”라고 표현한 적이 있었다. 처음 들었을 때는 재미있는 비유 정도로 넘겼지만, 이번에 `await`을 설명하려다 막히고 나니 그 표현이 다시 떠올랐다. 실제로 함수가 어딘가에 통째로 얼려져 보관된다는 뜻은 아니지만, 콜 스택을 계속 점유하지 않으면서도 멈췄던 흐름을 다시 이어간다는 구조를 떠올리는 데 좋은 실마리가 되었다.

이번 경험 이후로 모르는 지점을 발견하는 일이 조금 다르게 느껴졌다. 모르는 부분을 바로 정답으로 덮으려 하기보다, 내가 어디서 막혔는지 질문으로 만들어보는 과정도 학습이 될 수 있다는 것을 느꼈다.

앞으로도 비슷한 순간을 자주 마주하게 될 것 같다. 그때마다 틀린 가설을 세우는 일을 너무 두려워하지 않고, 검증 가능한 형태로 꺼내보려 한다. 그리고 혼자서 결론을 서두르기보다, 크루들과 함께 이야기하며 이해를 좁혀가는 방식을 계속 가져가고 싶다.
